In this task, I have created a basic sales summary using Python and SQLite. I connected to a SQLite database (sales_data.db) using the sqlite3 library and created a simple sales table with sample data. And Using an SQL query, I have retrieved the total quantity sold and total revenue for each product by grouping the results using the GROUP BY clause. I then loaded the query result into a pandas DataFrame, printed the summary, and visualized the revenue data with a bar chart using matplotlib. This task helped me practice SQL integration with Python, data summarization, and basic data visualization.
